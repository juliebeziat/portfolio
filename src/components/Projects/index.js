import DesktopMenu from '../Menu/DesktopMenu';
import './projects.scss';

const Projects = () => {
    return (
        <div>
            Projets
            <DesktopMenu />
        </div>
    );
};

export default Projects;
