// == Import style
import './menu.scss';

// == Import components
import MobileMenu from './MobileMenu';

const Menu = () => {
  return (
    <div>
      <MobileMenu />
    </div>
  );
};

export default Menu;
